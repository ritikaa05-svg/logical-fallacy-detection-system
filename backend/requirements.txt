# LogiScan Backend Dependencies (May 2026 Optimized)

# Web Framework
fastapi==0.139.2
uvicorn[standard]==0.51.0
starlette==1.3.1

# Data Validation
pydantic==2.13.4
pydantic-settings==2.14.2

# ML/AI - Standardized for 2026
--extra-index-url https://download.pytorch.org/whl/cpu
# CPU wheel pinned explicitly (local-version +cpu) so installs get the CPU build,
# regardless of CUDA availability — keeps the Docker image small and reproducible.
torch==2.13.0+cpu
transformers==5.14.1
onnxruntime==1.27.0
onnx==1.22.0
accelerate==1.14.0
bitsandbytes==0.49.2
optimum==2.2.0
scikit-learn==1.9.0
numpy==2.5.0
datasets==5.0.0
sentencepiece==0.2.2

# Tokenization
tiktoken==0.13.0
tokenizers==0.22.2

# Database & Cache
redis[hiredis]==8.0.1
asyncpg==0.31.0
sqlalchemy[asyncio]==2.0.51

# HTTP Client
aiohttp==3.14.2
httpx==0.28.1

# NLP Utilities - Updated for binary compatibility
spacy==3.8.13
# Note: Use the model downloader instead of a hardcoded URL to prevent build failures
# Command: python -m spacy download en_core_web_sm
textstat==0.7.13

# Utilities
python-dotenv==1.2.2
tenacity==9.1.4
aiofiles==25.1.0
python-multipart==0.0.32
jinja2==3.1.6
z3-solver==5.0.0.0
Pillow==12.3.0
pytesseract==0.3.13
psutil==7.2.2

# Phase 6 — Document Ingestion & Reporting
pymupdf==1.28.0
python-docx==1.2.0
pdfplumber==0.11.10
python-pptx==1.0.2
weasyprint==69.0
streamlit==1.60.0
plotly==6.8.0

# Observability / Metrics
prometheus-client==0.25.0

# Testing
pytest==9.1.1
pytest-asyncio==1.4.0
coverage==7.15.2
